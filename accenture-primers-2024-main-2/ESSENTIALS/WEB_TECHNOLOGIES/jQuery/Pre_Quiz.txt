1. When referencing an HTML element using jQuery preceded by a ‘ # ‘ , what JavaScript function is this equivalent to?
ANS: getElementById

2. The following elements are newly added elements in HTML5: 

<section>

<article>

<aside>
ANS: Semantic elements

3. Which of the following is most appropriate tag in html 5 to divide the document into logical document groups?
ANS: Which of the following is most appropriate tag in html 5 to divide the document into logical document groups?

4. If you want to change the color of a link to red when moving mouse pointer on top of it, which CSS property you need to change?
ANS: a:hover{ color: red; }


5. Which of the following is/are true about HTML?
ANS: Some of the tags are self closing while some of the tags must be explicitly closed in HTML5, Browser does not throw any error even if we have mistaken in the HTML syntax